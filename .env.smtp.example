# ===========================================
# SMTP Configuration for Email Sending
# ===========================================

# SMTP Server Configuration
# For Gmail: smtp.gmail.com
# For Outlook: smtp-mail.outlook.com
# For Yahoo: smtp.mail.yahoo.com
# For custom SMTP: your-smtp-server.com
SMTP_HOST=smtp.gmail.com

# SMTP Port
# 587 for TLS (recommended)
# 465 for SSL
# 25 for unencrypted (not recommended)
SMTP_PORT=587

# Use SSL/TLS
# true for port 465, false for port 587
SMTP_SECURE=false

# SMTP Authentication
# Your email address
SMTP_USER=your-email@gmail.com

# Your email password or app-specific password
# For Gmail: Use App Password (not your regular password)
#   1. Go to https://myaccount.google.com/security
#   2. Enable 2-Step Verification
#   3. Go to App Passwords
#   4. Generate a new app password
#   5. Use that password here
SMTP_PASS=your-app-specific-password

# From address (optional, defaults to SMTP_USER)
SMTP_FROM=noreply@duaab89.org

# ===========================================
# Gmail Configuration Example
# ===========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-16-digit-app-password
# SMTP_FROM=DU Alumni 89 <your-email@gmail.com>

# ===========================================
# Outlook Configuration Example
# ===========================================
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password
# SMTP_FROM=DU Alumni 89 <your-email@outlook.com>

# ===========================================
# Custom SMTP Configuration Example
# ===========================================
# SMTP_HOST=mail.yourcompany.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=admin@yourcompany.com
# SMTP_PASS=your-password
# SMTP_FROM=DU Alumni 89 <noreply@yourcompany.com>
